namespace InjectorGames.NetworkLibrary.UDP
{
    /// <summary>
    /// UDP request type
    /// </summary>
    public enum UdpRequestResponseType : byte
    {
        Connect,
        Disconnect,
        Count,
    }
}
