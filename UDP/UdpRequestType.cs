namespace InjectorGames.NetworkLibrary.UDP
{
    /// <summary>
    /// UDP request type
    /// </summary>
    public enum UdpRequestType : byte
    {
        Connect = 0,
        Disconnect = 1,
    }
}
